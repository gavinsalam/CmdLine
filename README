README for the CmdLine directory
--------------------------------

A simple library to provde access to command line options. See
example.cc for an illustration of how to use it.

type 

 make 

to build the library. 

In the svn version, there's also an autotools setup (in the unused/
directory), but it comes absolutely without guarantees.
